package com.example.aurora_checker;

public class AuroraBackend {

    final private int THRESHOLD = 0;
    public boolean isLikely(){
        //to be implemented, checks the 3-day forecast for auroras txt file from NOAA Api
        //returns true iff user's region displays a probability >= THRESHOLD
        return true;
    }
}
